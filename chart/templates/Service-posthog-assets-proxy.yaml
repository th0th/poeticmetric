{{ if .Values.postHog.enabled }}
apiVersion: v1
kind: Service
metadata:
  labels:
    {{ include "poeticmetric.labels" . | nindent 4 }}
  name: {{ include "poeticmetric.fullname" . }}-posthog-assets-proxy
spec:
  externalName: {{ .Values.postHog.assetsHostname }}
  type: ExternalName
{{ end }}
