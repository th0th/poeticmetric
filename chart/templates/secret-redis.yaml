apiVersion: v1
kind: Secret
metadata:
  labels:
    {{ include "poeticmetric.labels" . | nindent 4 }}
  name: {{ include "poeticmetric.fullname" . }}-redis
data:
  PASSWORD: {{ .Values.redis.password | required ".Values.redis.password is required!" | b64enc }}
