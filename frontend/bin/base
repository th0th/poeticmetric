#!/usr/bin/env bash
set -eo pipefail

get-base-dir() {
  echo "$(dirname $(dirname "$(readlink -f "$0")"))"
}

get-bin-dir() {
  echo "$(get-base-dir)/bin"
}

get-public-dir() {
  echo "$(get-base-dir)/public"
}
